```bash
@echo off
chcp 65001 >nul 2>nul
setlocal enabledelayedexpansion

:: 设置仓库路径
set "REPO_PATH=F:\Obsidian_灾后重建"

:: 检查仓库路径
if not exist "%REPO_PATH%\.git" (
    echo 错误：找不到Git仓库！请检查路径 "%REPO_PATH%"
    goto :END
)

:: 切换到仓库目录
cd /d "%REPO_PATH%" || (
    echo 错误：无法切换到仓库目录！
    goto :END
)

:: 设置安全目录
git config --local safe.directory "%REPO_PATH%" 2>nul

echo ==========================================
echo 开始自动提交Obsidian笔记到GitHub...
echo 当前时间: %date% %time%
echo ==========================================

:: 检测所有变更（包括未跟踪文件）
git status --porcelain | findstr /r /c:"^[MADRC?][ MADRC?]" >nul
if errorlevel 1 (
    echo 没有需要提交的更改，操作已取消。
    goto :END
)

:: 添加所有更改
echo 添加文件中...
git add . || (
    echo 错误：添加文件失败！
    goto :END
)

:: 获取标准化日期
for /f "tokens=1-3 delims=/- " %%a in ("%date%") do (
    set "year=%%a"
    set "month=%%b"
    set "day=%%c"
)

:: 创建提交信息
set "time_str=%time: =0%"          :: 处理小时空格
set "time_str=%time_str::0=:%"     :: 保持冒号分隔
set "COMMIT_MSG=自动提交：%year%-%month%-%day% %time_str:~0,8%"

:: 提交更改
echo 正在提交...
git commit -m "%COMMIT_MSG%" || (
    echo 错误：提交失败！可能没有实际更改。
    goto :END
)

:: 推送更新
:RETRY_PUSH
echo 正在推送到GitHub...
git push || (
    echo 错误：推送失败！尝试拉取远程变更...
    git pull --rebase && goto :RETRY_PUSH
    echo 错误：同步失败！请手动处理冲突
    goto :END
)

echo ==========================================
echo 操作成功完成！笔记已同步到GitHub。
echo 提交信息：%COMMIT_MSG%
echo ==========================================

:END
echo.
pause
```